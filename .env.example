# API Base URL (PUBLIC_ prefix required for client-side access in Astro)
# Format: https://api.domain.com?db=DATABASE_NAME
# Endpoints will be appended: /contact/messages, /contact/quotes
PUBLIC_API_URL=https://api.bard-dev.com?db=portfolio

# Docker / Traefik
DOMAIN=portfolio.example.com
